package com.aviato.demo.models;

public enum SeatType {
    //This will allow us to have only two types and standardize the seats available.
    //So we do not have the extra classes.
    ECONOMY,
    FIRST
}
